services:
  db:
    image: postgres:15-alpine # Pinning a stable version tag is best practice
    container_name: ecommerce-postgres-db
    restart: always
    environment:
      POSTGRES_DB: ecommerce_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: mysecretpassword
    ports:
      - "3000:5432"


